<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Mediamouse\Users\Enums\NoteStatus;
use Mediamouse\Users\Enums\NoteType;

return new class extends Migration
{
    public function up()
    {
        Schema::create('notes', function (Blueprint $table): void{
            $table->id();

            $table->bigInteger('has_notes_id')->unsigned();
            $table->string('has_notes_type');
            $table->bigInteger('assigned_to')->unsigned()->nullable();

            $table->datetimes();

            $table->dateTime('milestone_at')->nullable();

            $table->enum('type', NoteType::options());
            $table->enum('status', NoteStatus::options());
            $table->text('content');

            $table->foreign('assigned_to')->on('users')->references('id');
        });
    }

    public function down()
    {
        Schema::dropIfExists('notes');
    }
};

